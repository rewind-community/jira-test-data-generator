# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
responses>=0.25.0
aioresponses>=0.7.6

# Code quality
# Pinned to match .github/workflows/lint.yml exactly. A floating ruff means developers
# format with a different version than CI enforces. Keep these two in sync when bumping.
ruff==0.16.0
