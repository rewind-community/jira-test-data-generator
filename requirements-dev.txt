# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
responses>=0.25.0
aioresponses>=0.7.9

# Code quality
# This pin is the single source of truth for the ruff version: lint.yml installs ruff
# using this file as a pip constraints file, so CI and local development cannot drift.
# Bump it here and CI follows automatically.
ruff==0.16.0
